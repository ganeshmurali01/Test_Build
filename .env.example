# Public API base URL (used by fetcher.js)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Secret key for signing JWTs (min 32 chars, keep private)
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production

# Name of the HttpOnly cookie that stores the auth token
COOKIE_NAME=auth_token
